name: Security
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # <-- The Enterprise Best Practice

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  security:
    uses: CyberFvde/security-workflows/.github/workflows/security-pipeline.yaml@main
